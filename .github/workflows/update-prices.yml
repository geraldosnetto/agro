name: Update Prices

on:
  schedule:
    # Executar todo dia às 10:00 UTC (07:00 Horário de Brasília)
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Call Update API
        run: |
          if [ -z "${{ secrets.APP_URL }}" ]; then
            echo "Error: APP_URL secret is not set"
            exit 1
          fi
          
          echo "Triggering price update at ${{ secrets.APP_URL }}..."
          
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
            "${{ secrets.APP_URL }}/api/admin/update-prices?key=${{ secrets.CRON_SECRET }}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}")
            
          if [ "$HTTP_STATUS" -eq 200 ]; then
             echo "Success: API returned 200 OK"
          else
             echo "Error: API returned $HTTP_STATUS"
             exit 1
          fi
